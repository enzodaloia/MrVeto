{% extends 'base.html.twig' %}

{% block title %}Gestion des RDV{% endblock %}

{% block body %}
<div class="container-fluid px-0" data-controller="rendezvous" data-rendezvous-endpoint-prefix-value="/gestion-rdv">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center mb-4">
        <div>
            <h1 class="fw-black text-dark mb-1" style="font-weight: 900;">Gestion des RDV</h1>
            <p class="text-muted mb-0">Gerer vos consultations en quelques clics</p>
        </div>
    </div>

    <div class="bg-white rounded-4 shadow-sm p-4 mb-4">
        <div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-center mb-4 gap-3">
            <div class="bg-light rounded-pill p-1 d-inline-flex">
                <button data-rendezvous-target="btnUpcoming" data-action="click->rendezvous#showUpcoming" class="btn btn-white bg-white text-primary fw-bold rounded-pill px-4 py-2 shadow-sm border-0">
                    A venir ({{ upcoming|length }})
                </button>
                <button data-rendezvous-target="btnHistory" data-action="click->rendezvous#showHistory" class="btn text-muted fw-medium rounded-pill px-4 py-2 border-0 hover-bg-white">
                    Historique des consultations
                </button>
            </div>

            <div class="d-flex gap-2">
                <div class="input-group bg-light rounded-pill overflow-hidden">
                    <span class="input-group-text bg-transparent border-0 ps-3 pe-2 text-muted">
                        <i class="bi bi-search"></i>
                    </span>
                    <input type="text" data-rendezvous-target="searchInput" data-action="keyup->rendezvous#search" class="form-control bg-transparent border-0 shadow-none" placeholder="Rechercher un motif...">
                </div>
                <button data-rendezvous-target="btnSort" data-action="click->rendezvous#sort" class="btn btn-light rounded-circle d-flex align-items-center justify-content-center sort-asc" style="width: 40px; height: 40px;">
                    <i data-rendezvous-target="sortIcon" class="bi bi-sort-down text-muted fs-5"></i>
                </button>
            </div>
        </div>

        <div data-rendezvous-target="sectionUpcoming">
            <h5 class="fw-bold text-dark d-flex align-items-center gap-2 mb-3 mt-4">
                <i class="bi bi-calendar-check text-primary"></i> Consultations prevues
            </h5>

            <div class="d-flex flex-column gap-3 mb-5" data-rendezvous-target="upcomingContainer">
                {% for rdv in upcoming %}
                    <div class="card border border-light-subtle rounded-4 shadow-sm" data-rendezvous-target="upcomingItem" data-motif="{{ rdv.motif|default('Consultation')|lower }}" data-timestamp="{{ rdv.dateHeure.timestamp }}">
                        <div class="card-body p-3 d-flex flex-column flex-md-row align-items-md-center gap-3">
                            <div class="bg-neutral-10 rounded-3 d-flex flex-column align-items-center justify-content-center border" style="min-width: 80px; height: 80px;">
                                <span class="fs-4 fw-bold text-primary lh-1 mb-1">{{ rdv.dateHeure|date('d') }}</span>
                                <span class="text-muted fw-semibold" style="font-size: 0.85rem;">{{ rdv.dateHeure|date('M')|upper }}</span>
                            </div>

                            <div class="flex-grow-1">
                                <div class="d-flex align-items-center gap-2 mb-1">
                                    {% if rdv.statut == 'confirme' %}
                                        <span class="badge bg-success-bg text-success rounded-pill px-2 py-1 fw-bold" style="font-size: 0.7rem;">CONFIRME</span>
                                    {% elseif rdv.statut == 'en_attente' %}
                                        <span class="badge bg-warning-bg text-warning rounded-pill px-2 py-1 fw-bold" style="font-size: 0.7rem;">EN ATTENTE</span>
                                    {% else %}
                                        <span class="badge bg-light text-muted rounded-pill px-2 py-1 fw-bold" style="font-size: 0.7rem;">{{ rdv.statut|upper }}</span>
                                    {% endif %}
                                    <span class="text-muted" style="font-size: 0.85rem;">
                                        <i class="bi bi-clock"></i> {{ rdv.dateHeure|date('H:i') }}
                                    </span>
                                </div>
                                <h6 class="fw-bold text-dark mb-1 fs-5">{{ rdv.motif|default('Consultation') }}</h6>
                                <div class="text-muted d-flex align-items-center gap-1" style="font-size: 0.9rem;">
                                    <i class="bi bi-person"></i>
                                    {{ rdv.client.prenom|default('Client') }} {{ rdv.client.nom|default('') }}
                                    {% if rdv.animal %}
                                        • {{ rdv.animal.nom }}
                                    {% endif %}
                                </div>
                            </div>

                            <div class="d-flex gap-2 mt-3 mt-md-0">
                                {% if rdv.statut != 'confirme' %}
                                    <button class="btn btn-success text-white rounded-pill px-4 fw-medium border-0"
                                            data-action="click->rendezvous#confirmRdv"
                                            data-slug="{{ rdv.slug }}">
                                        Confirmer
                                    </button>
                                {% endif %}
                                <button class="btn btn-primary text-white rounded-pill px-4 fw-medium border-0"
                                        data-action="click->rendezvous#openEditModal"
                                        data-slug="{{ rdv.slug }}"
                                        data-vet="{{ rdv.client.prenom|default('Client') }} {{ rdv.client.nom|default('') }}"
                                        data-subtitle="Client"
                                        data-motif="{{ rdv.motif|default('Consultation') }}"
                                        data-statut="{{ rdv.statut }}"
                                        data-date="{{ rdv.dateHeure|date('Y-m-d') }}"
                                        data-time="{{ rdv.dateHeure|date('H:i') }}">
                                    Modifier
                                </button>
                                <button class="btn bg-danger-hover text-white rounded-pill px-4 fw-medium border-0 hover-bg-danger hover-text-white"
                                        data-action="click->rendezvous#openCancelModal"
                                        data-slug="{{ rdv.slug }}"
                                        data-date="{{ rdv.dateHeure|date('Y-m-d') }}"
                                        data-time="{{ rdv.dateHeure|date('H:i') }}">
                                    Annuler
                                </button>
                            </div>
                        </div>
                    </div>
                {% else %}
                    <div class="text-center py-4 bg-light rounded-4">
                        <p class="text-muted mb-0">Aucune consultation prevue pour le moment.</p>
                    </div>
                {% endfor %}
            </div>
        </div>

        <div data-rendezvous-target="sectionHistory">
            <div class="d-flex justify-content-between align-items-center mb-3 mt-5">
                <h5 data-rendezvous-target="historyTitle" class="fw-bold text-dark d-flex align-items-center gap-2 mb-0">
                    <i class="bi bi-clock-history text-muted"></i> Historique des consultations
                </h5>
            </div>
        </div>

        <form method="get" class="bg-light rounded-3 p-3 mb-3">
            <div class="row g-2 align-items-end">
                <div class="col-12 col-md-2">
                    <label class="form-label text-muted small mb-1" for="date_from">Date debut</label>
                    <input id="date_from" name="date_from" type="date" class="form-control" value="{{ filters.date_from }}">
                </div>
                <div class="col-12 col-md-2">
                    <label class="form-label text-muted small mb-1" for="date_to">Date fin</label>
                    <input id="date_to" name="date_to" type="date" class="form-control" value="{{ filters.date_to }}">
                </div>
                <div class="col-12 col-md-2">
                    <label class="form-label text-muted small mb-1" for="status">Statut</label>
                    <select id="status" name="status" class="form-select">
                        <option value="all" {% if filters.status == 'all' %}selected{% endif %}>Tous</option>
                        <option value="termine" {% if filters.status == 'termine' %}selected{% endif %}>Termine</option>
                        <option value="annule" {% if filters.status == 'annule' %}selected{% endif %}>Annule</option>
                    </select>
                </div>
                <div class="col-12 col-md-3">
                    <label class="form-label text-muted small mb-1" for="client">Client</label>
                    <input id="client" name="client" type="text" class="form-control" placeholder="Nom ou email" value="{{ filters.client }}">
                </div>
                <div class="col-12 col-md-2">
                    <label class="form-label text-muted small mb-1" for="animal">Animal</label>
                    <input id="animal" name="animal" type="text" class="form-control" placeholder="Nom" value="{{ filters.animal }}">
                </div>
                <div class="col-12 col-md-1 d-grid">
                    <button type="submit" class="btn btn-primary text-white fw-bold">Filtrer</button>
                </div>
            </div>
        </form>

        <div class="table-responsive">
            <table class="table table-borderless align-middle mb-0">
                <thead>
                    <tr class="border-bottom border-light-subtle">
                        <th class="text-muted fs-7 fw-bold pb-2" style="font-size: 0.75rem;">DATE</th>
                        <th class="text-muted fs-7 fw-bold pb-2" style="font-size: 0.75rem;">CLIENT</th>
                        <th class="text-muted fs-7 fw-bold pb-2" style="font-size: 0.75rem;">ANIMAL</th>
                        <th class="text-muted fs-7 fw-bold pb-2" style="font-size: 0.75rem;">MOTIF & STATUT</th>
                    </tr>
                </thead>
                <tbody data-rendezvous-target="historyContainer">
                    {% for rdv in history %}
                        <tr class="border-bottom border-light-subtle" data-rendezvous-target="historyItem" data-motif="{{ rdv.motif|default('Consultation')|lower }}" data-timestamp="{{ rdv.dateHeure.timestamp }}">
                            <td class="py-3">
                                <div class="fw-bold text-dark">{{ rdv.dateHeure|date('d M Y') }}</div>
                                <div class="text-muted" style="font-size: 0.75rem; text-transform: uppercase;">
                                    {% if rdv.statut == 'termine' or rdv.dateHeure < date() %}VISITE TERMINEE{% elseif rdv.statut == 'annule' %}ANNULE{% else %}{{ rdv.statut }}{% endif %}
                                </div>
                            </td>
                            <td class="py-3">
                                <div class="fw-bold text-dark">{{ rdv.client.prenom|default('Client') }} {{ rdv.client.nom|default('') }}</div>
                                <div class="text-muted" style="font-size: 0.85rem;">{{ rdv.client.email|default('') }}</div>
                            </td>
                            <td class="py-3">
                                <div class="fw-bold text-dark">{{ rdv.animal ? rdv.animal.nom : '---' }}</div>
                                <div class="text-muted" style="font-size: 0.85rem;">{{ rdv.animal ? rdv.animal.espece|default('') : '' }}</div>
                            </td>
                            <td class="py-3">
                                <div class="fw-bold text-dark">{{ rdv.motif|default('Consultation') }}</div>
                                <div class="text-muted" style="font-size: 0.85rem;">{{ rdv.remarque|default('Aucune remarque')|slice(0, 40) }}</div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4" class="text-center py-4 text-muted">Aucun historique disponible.</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <div class="modal fade" tabindex="-1" aria-hidden="true" data-rendezvous-target="cancelModal">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content rounded-4 border-0 shadow">
                <div class="modal-body p-4 p-md-5 text-center">
                    <div class="mb-4">
                        <div class="d-inline-flex align-items-center justify-content-center bg-danger text-white rounded-circle mb-3" style="width: 80px; height: 80px; background-color: #dc3545 !important;">
                            <i class="bi bi-x-circle" style="font-size: 2.5rem;"></i>
                        </div>
                    </div>
                    <h4 class="fw-bold text-dark mb-3">Annuler le rendez-vous</h4>
                    <p class="text-info text-muted mb-4">Êtes-vous sûr(e) de vouloir annuler ce rendez-vous ? L'annulation libérera le créneau :</p>

                    <div class="bg-light rounded-3 p-3 mb-4 d-flex justify-content-center gap-4 align-items-center">
                        <div class="d-flex align-items-center gap-2">
                            <i class="bi bi-calendar-event text-primary fs-5"></i>
                            <span class="text-info fw-bold text-dark" data-rendezvous-target="modalDate"></span>
                        </div>
                        <div class="border-start border-2 border-secondary" style="height: 30px; opacity: 0.2;"></div>
                        <div class="d-flex align-items-center gap-2">
                            <i class="bi bi-clock text-primary fs-5"></i>
                            <span class="text-info fw-bold text-dark" data-rendezvous-target="modalTime"></span>
                        </div>
                    </div>

                    <div class="d-flex flex-column flex-sm-row gap-2">
                        <button type="button" class="btn btn-info text-white border-0 rounded-3 fw-bold w-50 py-3" data-bs-dismiss="modal">
                            Conserver le rendez-vous
                        </button>
                        <button type="button" class="btn btn-danger text-white border-0 rounded-3 fw-bold w-50 py-3" data-action="click->rendezvous#confirmCancel" data-rendezvous-target="confirmCancelBtn">
                            Annuler le rendez-vous
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" tabindex="-1" aria-hidden="true" data-rendezvous-target="editModal">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content rounded-4 border-0 shadow">
                <div class="modal-body p-4 p-md-5 text-center">
                    <h3 class="fw-black text-info mb-2" style="font-weight: 800;">Modifier le Rendez-vous</h3>
                    <p class="text-info opacity-75 mb-4">Le client sera notifié de votre demande de modification</p>

                    <div class="d-flex align-items-center justify-content-center gap-4 mb-4 text-start">
                        <div class="rounded-circle overflow-hidden bg-light" style="width: 80px; height: 80px; flex-shrink: 0;">
                            <img src="" data-rendezvous-target="editModalVetImg" class="w-100 h-100 object-fit-cover" alt="Client">
                        </div>
                        <div>
                            <h5 class="fw-bold text-dark mb-1" data-rendezvous-target="editModalVetName">Client</h5>
                            <p class="text-muted mb-1" style="font-size: 0.9rem;" data-rendezvous-target="editModalVetSubtitle">Client</p>
                            <div class="d-flex align-items-center gap-2 mt-2">
                                <span class="text-info text-decoration-none" style="font-size: 0.85rem;" data-rendezvous-target="editModalMotif">Consultation</span>
                                <span class="text-muted">•</span>
                                <span class="badge bg-warning text-white rounded-pill px-2 py-1 fw-bold" style="font-size: 0.75rem;" data-rendezvous-target="editModalBadge">En attente</span>
                            </div>
                        </div>
                    </div>

                    <div class="d-flex justify-content-center gap-4 mb-5">
                        <div class="position-relative d-flex align-items-center border-bottom border-info pb-1" style="width: 250px;">
                            <i class="bi bi-calendar-event text-info fs-5 me-2"></i>
                            <select class="form-select border-0 bg-transparent text-info fw-bold p-0 shadow-none form-select-sm cursor-pointer" data-rendezvous-target="editModalDateSelect" data-action="change->rendezvous#onEditDateChange">
                                <option value="">Chargement...</option>
                            </select>
                        </div>
                        <div class="position-relative d-flex align-items-center border-bottom border-info pb-1" style="width: 250px;">
                            <i class="bi bi-clock text-info fs-5 me-2"></i>
                            <select class="form-select border-0 bg-transparent text-info fw-bold p-0 shadow-none form-select-sm cursor-pointer" data-rendezvous-target="editModalTimeSelect">
                                <option value="">--:--</option>
                            </select>
                        </div>
                    </div>

                    <div class="d-flex gap-3 mt-4">
                        <button type="button" class="btn btn-danger text-white border-0 rounded-3 fw-bold w-50 py-3" data-bs-dismiss="modal">
                            Retour
                        </button>
                        <button type="button" class="btn btn-info text-white border-0 rounded-3 fw-bold w-50 py-3" data-action="click->rendezvous#confirmEdit" data-rendezvous-target="confirmEditBtn">
                            Modifier mon rendez-vous
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
