{% extends 'base.html.twig' %}

{% block title %}Nouveau cabinet{% endblock %}

{% block body %}
<div class="container py-4" style="max-width: 900px;">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">Créer un cabinet</h1>
        <a class="btn btn-outline-secondary" href="{{ path('app_admin_cabinet_index') }}">Retour</a>
    </div>

    <div class="card border-0 shadow-sm rounded-4 p-4">
        {{ include('admin_cabinet/_form.html.twig', {form: form, button_label: 'Créer'}) }}
    </div>
</div>
{% endblock %}
